import { FC, HTMLAttributes } from 'react';
import { CrystallizeImageVariant } from '@crystallize/react-image';
declare global {
    interface navigator {
        connection: any;
    }
}
export interface Props extends HTMLAttributes<HTMLDivElement> {
    playlists: string[];
    thumbnails?: CrystallizeImageVariant[];
    thumbnailProps?: object;
    videoProps?: HTMLAttributes<HTMLVideoElement>;
    autoPlay?: boolean;
    loop?: boolean;
    muted?: boolean;
    controls?: boolean;
    poster?: string;
    playButtonText?: string;
}
export declare const Video: FC<Props>;
